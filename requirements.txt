# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.0
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0
pydub>=0.25.1
librosa>=0.10.0

# LLM inference
vllm>=0.3.0

# TTS model (install separately)
# pip install moss-ttsd

# Optional: for audio processing
scipy>=1.10.0
matplotlib>=3.7.0
